<div class='container'>
  <div class='row'>
    <div class='col-md-3'></div>
        <p id= "notice"></p>
        <div class='col-md-8 offset-md-1'>
            <h1>ユーザーが投稿した店舗詳細情報</h1>
            <table class = "table table-hover table-inverse">
              <thead>
                <tr>
                  <th>投稿者名</th>
                  <th>店名</th>
                  <th>最寄り駅名</th>
                  <th>投稿者の感想</th>
                  <th>店舗写真</th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
              <tr>
                <td><%= @shop.end_user.name %></td>
                <td><%= @shop.title %></td>
                <td><%= @shop.access %></td>
                <td><%= @shop.body %></td>
                <td>
                <% if @shop.image.attached? %>
                  <%= image_tag @shop.image, size: "200x200" %>
                <% else %>
                  <%= image_tag 'no_image', size: "200x200" %>
                <% end %>
                </td>
              </tr>
              </tbody>

              <div>
                <%= link_to "内容が不適切なので削除する", admin_shop_path(@shop.id), method: :delete %>
              </div>

            </table>
        </div>
    </div>
</div>
