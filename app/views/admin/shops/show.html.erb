<div class='container'>
  <div class='row'>
    <div class='col-md-3'></div>
        <p id= "notice"></p>
        <div class>
            <h1>ユーザーが投稿した店舗詳細情報</h1>
            <table class = "table table-hover table-inverse">
              <tbody>
                <tr>
                  <td>投稿者名</td>
                  <td><%= @shop.end_user.name %></td>
                </tr>
                <tr></tr>
                  <td>店名</td>
                  <td><%= @shop.title %></td>
                </tr>
                <tr>
                  <td>最寄り駅名</td>
                  <td><%= @shop.access %></td>
                </tr>
                <tr>
                  <td>投稿者の感想</td>
                  <td><%= @shop.body %></td>
                </tr>
                <tr>
                  <td>店舗写真</td>
                  <td>
                    <% if @shop.image.attached? %>
                      <%= image_tag @shop.image, size: "100x100" %>
                    <% else %>
                      <%= image_tag 'no_image.jpg', size: "100x100" %>
                    <% end %>
                  </td>
                </tr>
                <tr>
                  <td>☆レビュー数</td>
                  <td><%= @shop.rate %></td>
                </tr>
              </tbody>

              <div>
                <!--#↓削除前に注意文を出す-->
                <%= link_to "内容が不適切なので削除する", admin_shop_path(@shop.id), method: :delete, data: { confirm: "本当にこの投稿を削除しますか?" } %>
              </div>

            </table>
        </div>
    </div>
</div>
