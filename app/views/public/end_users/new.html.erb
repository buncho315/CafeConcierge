<h1>エンドユーザーのnew いるかな？</h1>
<%= form_with model: @shop, url:　, method: :post do |f| %>

  <h4>店名</h4>
  <%= f.text_field :title %>

  <h4>店舗説明(300文字以内)</h4>
  <%= f.text_area :body %>

  <%= f.submit '投稿する' %>
<% end %>